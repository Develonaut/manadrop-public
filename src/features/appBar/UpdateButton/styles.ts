import { makeStyles, Theme } from "@material-ui/core/styles";

export const styles = makeStyles((theme: Theme) => {
  return {
    button: {},
    icon: {
      width: 20,
      height: 20,
      color: theme.palette.common.forest
    }
  };
});
